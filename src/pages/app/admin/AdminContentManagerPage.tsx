// Backward-compatible admin entrypoint. Plan activation now lives in the
// dedicated subscription screen and intentionally contains no model controls.
export { default } from './SubscriptionManagementPage';
