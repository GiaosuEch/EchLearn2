import assert from 'node:assert/strict'; import { describe, it } from 'node:test';
import { buildLocalModelArtifactSelectionPersistenceEnvelope } from '../../src/platform/ai/localModelArtifactSelectionPersistencePolicy.ts';
import { LOCAL_MODEL_ARTIFACT_SELECTION_RECORD_SELECT_COLUMNS, verifyLocalModelArtifactSelectionRecordRow } from '../../src/platform/ai/localModelArtifactSelectionRecordVerification.ts';
const rev = 'c1899de289a04d12100db370d81485cdf75e47ca';
const envelope = buildLocalModelArtifactSelectionPersistenceEnvelope({ trustedDecision: { status:'trusted-selection-decision-ready',trustedSelectionDecisionReady:true,reviewVerified:true,selectionVerified:true,trustedReviewerVerified:true }, bridgeDecision:{status:'eligible-for-artifact-selection-review',bridgeEligible:true,artifactSelectionReviewEligible:true,bridgeDecisionKey:'b',applicationDecisionKey:'a',sourceGovernancePersistenceKey:'g',canonicalRecordKey:'r',canonicalRecordRevision:1,canonicalOutcome:'finalized-proceed',candidateId:'qwen3-0-6b-candidate',candidateTier:'light',observedRevision:rev}, selectionResult:{status:'selection-recorded',decision:'select',humanSelectionRecorded:true,selectionValidForCurrentScope:true,artifactSelected:true,selectedOptionId:'o',selectedScope:{candidateId:'qwen3-0-6b-candidate',candidateTier:'light',observedRevision:rev},availableOptions:[{optionId:'o',candidateId:'qwen3-0-6b-candidate',candidateTier:'light',observedRevision:rev}]}}); assert.ok(envelope);
describe('Phase 7.7 persisted selection record verification',()=>{it('verifies only an exact allowlisted immutable row',()=>{const row:any={id:'1',selection_decision_key:envelope.selectionDecisionKey,selection_idempotency_key:envelope.selectionIdempotencyKey,schema_revision:1,selection_policy_revision:1,bridge_decision_key:'b',source_application_decision_key:'a',source_governance_persistence_key:'g',canonical_record_key:'r',canonical_record_revision:1,canonical_outcome:'finalized-proceed',candidate_id:'qwen3-0-6b-candidate',candidate_tier:'light',observed_revision:rev,selected_option_id:'o',selection_decision:'select',selection_status:'selection-recorded',human_selection_recorded:true,selection_actor_user_id:'00000000-0000-4000-8000-000000000001',selection_scope:envelope.selectionScope,selection_envelope:envelope,created_at:'2026-07-21T00:00:00Z'};assert.equal(Object.keys(row).length,LOCAL_MODEL_ARTIFACT_SELECTION_RECORD_SELECT_COLUMNS.split(',').length);assert.equal(verifyLocalModelArtifactSelectionRecordRow(envelope,row).verified,true);assert.equal(verifyLocalModelArtifactSelectionRecordRow(envelope,{...row,observed_revision:'other'}).verified,false);});});
